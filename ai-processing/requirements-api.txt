# PrismWeave RAG API Server Requirements
# Install with: uv pip install -r requirements-api.txt

# Core API framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# HTTP client for external requests  
httpx>=0.25.2
aiofiles>=23.2.0

# Request validation and serialization
pydantic>=2.5.0
pydantic-settings>=2.0.0

# CORS middleware and file uploads
python-multipart>=0.0.6

# JSON handling (faster than standard json)
orjson>=3.9.0

# Optional: for better development experience
python-dotenv>=1.0.0

# LangChain for enhanced text processing and embedding
langchain>=0.1.0
langchain-community>=0.0.10
langchain-text-splitters>=0.0.1

# Note: Additional LangChain dependencies for specific features:
# uv pip install langchain-chroma langchain-ollama  # For specific integrations
