// Generated by Copilot
// PrismWeave Markdown Conversion - Public API
//
// This module provides a clean, well-documented interface for HTML-to-markdown
// conversion within the PrismWeave browser extension.

// Primary class for browser extension contexts
export { MarkdownConverter } from '../markdown-converter';

// Core conversion engine (for advanced use cases and testing)
export { MarkdownConverterCore } from '../markdown-converter-core';

// Type definitions
export type { IConversionOptions, IConversionResult } from '../markdown-converter-core';

/**
 * Quick Start Guide:
 *
 * Basic Usage:
 * ```typescript
 * import { MarkdownConverter } from './utils/markdown';
 *
 * const converter = new MarkdownConverter();
 * const result = converter.convertToMarkdown(htmlContent, {
 *   preserveFormatting: true,
 *   includeMetadata: true
 * });
 *
 * console.log(result.markdown);
 * console.log(`Word count: ${result.wordCount}`);
 * ```
 *
 * Testing with Core:
 * ```typescript
 * import { MarkdownConverterCore } from './utils/markdown';
 *
 * // Direct core usage for testing (requires manual TurndownService setup)
 * const core = new MarkdownConverterCore();
 * ```
 *
 * Architecture:
 * - MarkdownConverter: Browser-specific adapter with auto-initialization
 * - MarkdownConverterCore: Environment-agnostic conversion engine
 * - Use MarkdownConverter for all browser extension code
 * - Use MarkdownConverterCore only for testing or custom adapters
 */
