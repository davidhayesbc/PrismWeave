// Generated by Copilot
// Main bookmarklet script entry point - generates the hosted bookmarklet.js file

import { BookmarkletGenerator } from '../utils/bookmarklet-generator';

// Generate and output the main bookmarklet script
const mainScript = BookmarkletGenerator.generateMainBookmarkletScript();

// Output the script directly (this will be written to bookmarklet.js)
console.log(mainScript);

// Also make it available as a module export
export { mainScript };

// For direct script execution, write to document if in browser context
if (typeof document !== 'undefined') {
  // This is being loaded directly in a browser
  const script = document.createElement('script');
  script.textContent = mainScript;
  document.head.appendChild(script);
}
