// Generated by Copilot
// Simplified test to verify direct import from browser extension core

console.log('Starting direct import test...');

try {
  // Test the core import
  console.log('Attempting to import MarkdownConverterCore...');
  const { MarkdownConverterCore } = await import('../src/utils/markdown-converter-core.ts');
  console.log('✅ MarkdownConverterCore imported successfully');
  console.log('Class definition:', typeof MarkdownConverterCore);

  // Test the types import
  console.log('Attempting to import types...');
  const types = await import('../src/types/index.ts');
  console.log('✅ Types imported successfully');
  console.log('Available exports:', Object.keys(types));

  // Try to instantiate the class
  console.log('Attempting to create instance...');
  const core = new MarkdownConverterCore();
  console.log('✅ Core instance created successfully');
  console.log('Is initialized:', core.isInitialized);
} catch (error) {
  console.error('❌ Import test failed:', error);
  console.error('Error stack:', error.stack);
}
