// Generated by Copilot
// Direct URL test for the NodeMarkdownConverter

import { NodeMarkdownConverter } from './node-markdown-converter.ts';

async function testUrl() {
  console.log('🚀 Starting URL conversion test...');

  try {
    const converter = new NodeMarkdownConverter();
    console.log('✅ Converter created');

    // Test with the Docker blog URL
    const testUrl =
      'https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/';
    console.log(`📡 Converting URL: ${testUrl}`);

    const result = await converter.convertUrl(testUrl, {
      generateFrontmatter: true,
      includeMetadata: true,
    });

    console.log('✅ URL conversion successful!');
    console.log('📝 Markdown length:', result.markdown.length);
    console.log('📊 Word count:', result.wordCount);
    console.log('🖼️ Images found:', result.images.length);
    console.log('📋 First 200 chars of markdown:');
    console.log(result.markdown.substring(0, 200));
    console.log('---');

    if (result.frontmatter) {
      console.log('📄 Frontmatter:');
      console.log(result.frontmatter);
    }
  } catch (error) {
    console.error('❌ URL test failed:', error);
    console.error('Stack:', error instanceof Error ? error.stack : String(error));
  }
}

testUrl();
