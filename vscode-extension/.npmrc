# NPM configuration for cross-platform compatibility
optional = true
# Skip optional dependencies that fail to install
save-optional = false
# Continue installation even if optional dependencies fail
ignore-scripts = false
# Allow installation to continue if optional deps fail
# Set strict SSL to false for corporate environments (if needed)
# strict-ssl = false
